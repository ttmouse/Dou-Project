# .trees 并行开发实战示例

## 示例场景
假设你需要同时开发两个功能：
1. 为项目添加**评分系统**
2. 修复**标签同步bug**

## 🚀 实战演练

### 第一步：初始化

```bash
# 查看当前状态
./trees-manager.sh status
# 输出: 没有活动分支

# 查看所有分支
./trees-manager.sh list
# 输出: 所有开发分支: (空)
```

### 第二步：创建评分功能分支

```bash
# 创建评分功能分支
./trees-manager.sh create project_rating

# 输出:
# 成功创建分支: project_rating
# 使用 './trees-manager.sh switch project_rating' 切换到此分支

# 切换到该分支
./trees-manager.sh switch project_rating

# 输出:
# 已切换到分支: project_rating
```

### 第三步：开始开发评分功能

```bash
# 检查当前状态
./trees-manager.sh status

# 输出:
# 当前分支: project_rating
# 最近变更:
# 
# Git 状态:
# (空)
```

现在开始编码，假设你：
1. 创建了 `RatingView.swift` 文件
2. 修改了 `Project.swift` 添加评分属性
3. 更新了 `ProjectCard.swift` 显示评分

```bash
# 保存第一阶段进度
./trees-manager.sh save "添加评分数据模型和基础界面"

# 输出:
# 进度已保存: 添加评分数据模型和基础界面
```

继续开发，实现评分逻辑：

```bash
# 保存第二阶段进度  
./trees-manager.sh save "实现评分保存和读取功能"

# 输出:
# 进度已保存: 实现评分保存和读取功能
```

### 第四步：创建Bug修复分支

现在你发现了一个紧急的标签同步bug，需要立即修复，但评分功能还没完成。

```bash
# 创建bug修复分支
./trees-manager.sh create fix_tag_sync

# 切换到bug修复分支
./trees-manager.sh switch fix_tag_sync

# 输出:
# 已切换到分支: fix_tag_sync
```

开始修复bug：

```bash
# 定位问题
./trees-manager.sh save "定位标签同步问题，发现原因在TagSystemSync.swift"

# 修复问题
./trees-manager.sh save "修复标签丢失问题，更新同步逻辑"

# 测试修复
./trees-manager.sh save "测试标签同步，确认bug已修复"
```

### 第五步：提交Bug修复

```bash
# Bug修复完成，提交到Git
git add .
git commit -m "fix: 修复标签同步丢失问题

- 修复TagSystemSync中的同步逻辑错误
- 确保标签在应用重启后保持一致
- 添加错误处理和日志记录

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>"

# 合并到主分支
git checkout main
git merge fix_tag_sync

# 删除已完成的分支
./trees-manager.sh delete fix_tag_sync
```

### 第六步：继续开发评分功能

```bash
# 切换回评分功能分支
./trees-manager.sh switch project_rating

# 输出:
# 已切换到分支: project_rating

# 查看之前的进度
./trees-manager.sh status

# 输出:
# 当前分支: project_rating
# 最近变更:
#   [2024-08-24T10:45:00+07:00] 添加评分数据模型和基础界面
#   [2024-08-24T11:00:00+07:00] 实现评分保存和读取功能
```

继续完成评分功能：

```bash
# 添加评分UI完善
./trees-manager.sh save "完善评分界面，添加星级显示"

# 添加评分统计功能
./trees-manager.sh save "添加项目评分统计和排序功能"

# 最终测试
./trees-manager.sh save "完成评分功能开发和测试"
```

### 第七步：完成评分功能

```bash
# 提交评分功能
git add .
git commit -m "feat: 添加项目评分系统

- 实现1-5星评分功能
- 添加评分显示和编辑界面
- 支持按评分排序项目
- 集成评分数据到项目模型

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>"

# 合并到主分支
git checkout main
git merge project_rating

# 清理分支
./trees-manager.sh delete project_rating
git branch -d project_rating
```

### 第八步：查看最终状态

```bash
# 查看所有分支状态
./trees-manager.sh list

# 输出:
# 所有开发分支:
# (空)

# 查看Git提交历史
git log --oneline -5

# 输出:
# abc1234 feat: 添加项目评分系统
# def5678 fix: 修复标签同步丢失问题
# ...
```

## 📊 工作流程总结

### 你同时完成了什么？
1. **修复了紧急bug** - 标签同步问题得到解决
2. **开发了新功能** - 项目评分系统完整实现
3. **没有相互干扰** - 两个任务独立进行，互不影响

### 关键优势
- **灵活性** - 可以随时在不同任务间切换
- **可追溯性** - 每个分支都有完整的开发历史
- **安全性** - 每个分支的修改都有备份
- **清晰性** - 每个功能有独立的提交历史

## 🔍 分支状态演示

### 分支信息示例

在开发过程中，查看 `.trees/project_rating/info.json`：
```json
{
    "name": "project_rating",
    "created": "2024-08-24T10:30:00+07:00",
    "description": "",
    "status": "active",
    "commits": []
}
```

### 变更日志示例

查看 `.trees/project_rating/changes.log`：
```
[2024-08-24T10:45:00+07:00] 添加评分数据模型和基础界面
[2024-08-24T11:00:00+07:00] 实现评分保存和读取功能
[2024-08-24T14:30:00+07:00] 完善评分界面，添加星级显示
[2024-08-24T15:15:00+07:00] 添加项目评分统计和排序功能
[2024-08-24T16:00:00+07:00] 完成评分功能开发和测试
```

### 文件备份结构

```
.trees/project_rating/modified_files/
├── 20240824_104500/
│   ├── Sources/ProjectManager/Models/Project.swift
│   └── Sources/ProjectManager/Views/RatingView.swift
├── 20240824_110000/
│   ├── Sources/ProjectManager/Models/Project.swift
│   ├── Sources/ProjectManager/Views/RatingView.swift
│   └── Sources/ProjectManager/Views/ProjectCard.swift
└── ...
```

## 🎓 学到的技巧

### 1. 任务优先级管理
- 紧急bug可以打断功能开发
- 使用不同分支避免代码混乱
- 修复完bug后能无缝回到功能开发

### 2. 进度追踪
- 每个重要节点都要保存进度
- 使用描述性的保存消息
- 定期提交到Git作为检查点

### 3. 分支管理
- 完成的分支及时删除
- 保持工作区整洁
- 使用清晰的分支命名

### 4. 团队协作
- 每个人可以有自己的分支
- 通过Git合并实现协作
- 避免在主分支上直接开发

## 🎯 下一步

现在你已经了解了完整的工作流程，可以开始实际使用：

```bash
# 创建你的第一个分支
./trees-manager.sh create my_first_feature

# 开始开发之旅！
./trees-manager.sh switch my_first_feature
```

记住：**实践是最好的学习方式**！