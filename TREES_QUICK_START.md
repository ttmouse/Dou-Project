# 🌲 Trees 快速开始指南

## 🎯 三种使用方式

### 1. 交互式菜单（最简单）

```bash
./trees-interactive.sh
```

看到菜单后：
- 输入 `1` 创建分支
- 输入 `2` 切换分支  
- 输入 `3` 查看分支
- 输入 `0` 退出

### 2. 命令行方式

```bash
./trees-manager.sh create my_feature    # 创建
./trees-manager.sh switch my_feature    # 切换
./trees-manager.sh list                 # 查看
./trees-manager.sh delete my_feature    # 删除
```

### 3. 直接 Git 命令

```bash
git worktree add .trees/my_feature      # 创建
cd .trees/my_feature                    # 切换
git worktree list                       # 查看
git worktree remove .trees/my_feature   # 删除
```

## 🤔 哪种最简单？

### 对于新手：**交互式菜单**
- 有图形界面指导
- 不需要记命令
- 有错误检查和提醒

### 对于熟悉命令行：**trees-manager.sh**
- 快速简洁
- 一行命令搞定
- 有额外功能（状态显示、安全检查）

### 对于Git专家：**直接Git命令**
- 最原生
- 无依赖
- 完全标准

## 🚀 推荐工作流

1. **第一次使用** - 用交互式菜单熟悉
2. **日常工作** - 用 trees-manager.sh 命令
3. **特殊需求** - 直接用 Git 命令

## 💡 实际效果

无论用哪种方式，最终效果都是：

```bash
# 创建后
./trees-manager.sh switch my_feature

# 终端变成：
[my_feature] douba …/.trees/my_feature ❯
```

你在独立的工作环境中，可以：
- 修改代码不影响主分支
- 编译测试独立进行
- Git提交到专门分支

试试最简单的交互式菜单开始吧！

```bash
./trees-interactive.sh
```