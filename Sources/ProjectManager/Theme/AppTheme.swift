import SwiftUI

enum AppTheme {
    static let background = Color(.windowBackgroundColor)
    static let secondaryBackground = Color(.controlBackgroundColor)
    static let accent = Color.white.opacity(0.9)
    static let text = Color.primary
    static let secondaryText = Color.secondary
    static let border = Color.gray.opacity(0.2)
    
    static let cardBackground = Color(.controlBackgroundColor)
    static let cardShadow = Color.black.opacity(0.2)
    static let cardHover = Color.gray.opacity(0.1)
    
    static let tagBackground = Color.white.opacity(0.1)
    static let tagText = Color.white.opacity(0.9)
    
    static let buttonBackground = Color.white.opacity(0.1)
    static let buttonHover = Color.white.opacity(0.15)
} 