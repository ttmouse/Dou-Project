# ProjectManager 并行开发指南

## 概述

`.trees` 系统是 ProjectManager 的并行开发解决方案，允许同时进行多个独立的开发任务而不相互干扰。每个"分支"代表一个独立的开发任务或功能。

## 🎯 核心概念

### 什么是 .trees 分支？
- **分支** = 一个独立的开发任务（如添加新功能、修复bug等）
- **隔离** = 每个分支的代码修改互不影响
- **追踪** = 记录每个分支的进度和变更历史
- **合并** = 完成后将分支的修改合并到主代码

### 目录结构
```
.trees/
├── .current                    # 当前活动分支
├── search_feature/             # 搜索功能分支
│   ├── info.json              # 分支信息
│   ├── changes.log            # 变更日志
│   └── modified_files/        # 修改文件备份
├── theme_update/              # 主题更新分支
└── bug_fixes/                 # Bug修复分支
```

## 🚀 快速开始

### 1. 准备工作
```bash
# 确保脚本可执行
chmod +x trees-manager.sh

# 查看帮助
./trees-manager.sh help
```

### 2. 创建第一个分支
```bash
# 创建新功能分支
./trees-manager.sh create search_feature

# 切换到新分支
./trees-manager.sh switch search_feature
```

### 3. 开始开发
```bash
# 查看当前状态
./trees-manager.sh status

# 修改代码...
# 保存进度
./trees-manager.sh save "添加基础搜索界面"
```

## 📚 详细操作指南

### 创建新分支

#### 命令
```bash
./trees-manager.sh create <分支名>
```

#### 示例
```bash
# 创建功能分支
./trees-manager.sh create add_dashboard
./trees-manager.sh create fix_tag_sync
./trees-manager.sh create ui_improvement

# 分支命名建议
# - 功能: feature_xxx, add_xxx
# - 修复: fix_xxx, bug_xxx  
# - 改进: improve_xxx, update_xxx
```

#### 最佳实践
- 使用描述性名称（如 `add_project_search` 而不是 `feature1`）
- 一个分支专注于一个任务
- 避免在分支名中使用空格和特殊字符

### 切换分支

#### 命令
```bash
./trees-manager.sh switch <分支名>
```

#### 示例
```bash
# 切换到搜索功能分支
./trees-manager.sh switch search_feature

# 切换到bug修复分支  
./trees-manager.sh switch fix_tag_sync
```

#### 切换前检查清单
- [ ] 当前修改已保存到git或使用save命令
- [ ] 没有未完成的编译错误
- [ ] 重要文件已备份

### 查看分支状态

#### 列出所有分支
```bash
./trees-manager.sh list
```

输出示例：
```
所有开发分支:

  * search_feature (当前分支)
    创建时间: 2024-08-24T10:30:00+07:00
    描述: 添加项目搜索功能

    add_dashboard  
    创建时间: 2024-08-24T09:15:00+07:00
    描述: 
```

#### 查看当前状态
```bash
./trees-manager.sh status
```

输出示例：
```
当前分支: search_feature

最近变更:
  [2024-08-24T10:45:00+07:00] 添加搜索界面基础结构
  [2024-08-24T11:00:00+07:00] 实现搜索算法
  [2024-08-24T11:15:00+07:00] 添加搜索结果显示

Git 状态:
 M Sources/ProjectManager/Views/SearchView.swift
?? Sources/ProjectManager/Models/SearchManager.swift
```

### 保存开发进度

#### 命令
```bash
./trees-manager.sh save "<进度描述>"
```

#### 示例
```bash
# 保存功能开发进度
./trees-manager.sh save "完成搜索界面布局"
./trees-manager.sh save "实现搜索过滤逻辑" 
./trees-manager.sh save "添加搜索结果排序"

# 保存bug修复进度
./trees-manager.sh save "定位标签同步问题"
./trees-manager.sh save "修复标签丢失bug"
```

#### 保存做了什么？
1. **记录时间点** - 在changes.log中添加时间戳记录
2. **备份文件** - 将当前修改的文件备份到分支目录
3. **创建快照** - 为后续恢复提供安全点

### 提交到Git

#### 基本流程
```bash
# 1. 保存当前分支进度
./trees-manager.sh save "完成XXX功能开发"

# 2. 添加修改到git暂存区
git add .

# 3. 提交修改
git commit -m "feat: 实现XXX功能

- 添加搜索界面
- 实现搜索算法  
- 优化搜索性能

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

#### 提交信息规范
```bash
# 功能提交
git commit -m "feat: 添加项目搜索功能"

# 修复提交  
git commit -m "fix: 修复标签同步问题"

# 改进提交
git commit -m "improve: 优化界面响应速度"

# 文档提交
git commit -m "docs: 更新开发指南"
```

### 合并分支

#### 准备合并
```bash
# 1. 确保所有更改已提交
git status

# 2. 检查分支状态
./trees-manager.sh status

# 3. 最后一次保存进度
./trees-manager.sh save "分支开发完成，准备合并"
```

#### 合并到主分支
```bash
# 1. 使用脚本标记分支为已合并
./trees-manager.sh merge search_feature

# 2. 手动执行git合并
git checkout main
git merge search_feature

# 3. 如果有冲突，解决冲突后提交
git add .
git commit -m "resolve: 解决合并冲突"
```

#### 合并后清理
```bash
# 删除已合并的分支
./trees-manager.sh delete search_feature

# 删除git分支（如果有）
git branch -d search_feature
```

## ⚠️ 冲突处理

### 什么是冲突？
当两个分支修改了同一个文件的同一部分时，Git无法自动合并，需要手动解决。

### 识别冲突
```bash
git merge feature_branch
```

如果有冲突，会看到：
```
自动合并 Sources/ProjectManager/Views/ProjectListView.swift
冲突（内容）：合并冲突于 Sources/ProjectManager/Views/ProjectListView.swift
自动合并失败，修正冲突然后提交修正的结果。
```

### 解决冲突步骤

#### 1. 查看冲突文件
```bash
git status
```

#### 2. 编辑冲突文件
冲突标记格式：
```swift
<<<<<<< HEAD
// 主分支的代码
func searchProjects() {
    // 原有搜索逻辑
}
=======
// 你的分支的代码  
func searchProjects(query: String) {
    // 新的搜索逻辑
}
>>>>>>> feature_branch
```

#### 3. 手动合并
删除冲突标记，保留正确的代码：
```swift
func searchProjects(query: String) {
    // 合并后的最终代码
    // 保留两个分支的优点
}
```

#### 4. 标记冲突已解决
```bash
git add Sources/ProjectManager/Views/ProjectListView.swift
```

#### 5. 完成合并
```bash
git commit -m "resolve: 合并搜索功能分支，解决冲突"
```

### 预防冲突的技巧
1. **频繁同步** - 定期从主分支拉取更新
2. **小步提交** - 避免大批量修改
3. **沟通协调** - 团队成员避免同时修改同一文件
4. **模块化** - 不同功能尽量放在不同文件中

## 🔄 完整工作流程示例

### 场景：添加项目标签颜色功能

#### 1. 创建和切换分支
```bash
# 创建分支
./trees-manager.sh create tag_colors
# 输出: 成功创建分支: tag_colors

# 切换到分支
./trees-manager.sh switch tag_colors  
# 输出: 已切换到分支: tag_colors
```

#### 2. 开发第一阶段
```bash
# 开始编码...
# 修改 TagView.swift，添加颜色选择器

# 保存进度
./trees-manager.sh save "添加标签颜色选择界面"
```

#### 3. 继续开发
```bash
# 继续编码...
# 修改 TagManager.swift，添加颜色管理逻辑

# 保存进度  
./trees-manager.sh save "实现标签颜色存储和读取"
```

#### 4. 测试和调试
```bash
# 测试功能...
# 修复发现的问题

# 保存进度
./trees-manager.sh save "修复颜色显示bug，完成基本功能"
```

#### 5. 提交到Git
```bash
# 添加所有修改
git add .

# 提交修改
git commit -m "feat: 添加标签颜色功能

- 添加颜色选择器界面
- 实现颜色存储机制  
- 支持自定义标签颜色

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

#### 6. 合并到主分支
```bash
# 标记为准备合并
./trees-manager.sh merge tag_colors

# 切换到主分支
git checkout main

# 合并功能分支
git merge tag_colors

# 如果没有冲突，合并完成
# 如果有冲突，按照冲突处理步骤解决
```

#### 7. 清理分支
```bash
# 删除.trees分支
./trees-manager.sh delete tag_colors

# 删除git分支  
git branch -d tag_colors
```

## 🛠 高级技巧

### 1. 分支描述管理
```bash
# 手动编辑分支描述
nano .trees/tag_colors/info.json

# 修改description字段
{
    "name": "tag_colors",
    "created": "2024-08-24T10:30:00+07:00",
    "description": "为项目标签添加自定义颜色功能",
    "status": "active",
    "commits": []
}
```

### 2. 批量备份
```bash
# 备份所有分支数据
./trees-manager.sh backup
# 输出: 已创建备份文件: trees-backup-20240824_103000.tar.gz
```

### 3. 恢复备份
```bash
# 解压备份文件
tar -xzf trees-backup-20240824_103000.tar.gz
```

### 4. 查看分支历史
```bash
# 查看特定分支的变更日志
cat .trees/tag_colors/changes.log
```

### 5. 手动清理
```bash
# 清理空分支
find .trees -type d -empty -delete

# 清理临时文件
find .trees -name "*.tmp" -delete
```

## 📋 最佳实践检查清单

### 开始新分支前
- [ ] 当前工作已保存或提交
- [ ] 从最新的主分支创建
- [ ] 分支名称清晰描述任务
- [ ] 添加分支描述

### 开发过程中
- [ ] 定期保存进度（每30-60分钟）
- [ ] 提交信息清晰描述修改
- [ ] 避免在一个分支中做多个不相关的任务
- [ ] 定期同步主分支更新

### 合并前准备
- [ ] 功能完整并通过测试
- [ ] 代码符合项目规范
- [ ] 解决所有编译错误和警告
- [ ] 更新相关文档
- [ ] 最后一次完整测试

### 合并后
- [ ] 验证主分支功能正常
- [ ] 删除已合并分支
- [ ] 更新项目文档
- [ ] 通知团队成员

## 🆘 常见问题

### Q: 分支切换后代码消失了？
A: 检查是否正确切换分支，代码可能在其他分支中。使用 `./trees-manager.sh list` 查看所有分支。

### Q: 合并时出现大量冲突怎么办？
A: 建议先将主分支的更新合并到你的分支中，在分支中解决冲突，然后再合并到主分支。

### Q: 不小心删除了分支怎么办？
A: 如果有备份文件，可以恢复。如果提交到了Git，可以从Git历史中恢复。

### Q: 怎么在分支间传递部分修改？
A: 使用Git的cherry-pick功能，或者手动复制需要的代码片段。

### Q: 分支太多了怎么管理？
A: 定期清理已合并分支，为分支添加清晰描述，使用一致的命名规范。

## 📞 获得帮助

- 查看脚本帮助：`./trees-manager.sh help`
- 查看Git帮助：`git help <命令>`
- 项目文档：查看 CLAUDE.md 和 README.md

---

**记住**：并行开发的核心是保持每个分支的独立性和清晰的目标。当有疑问时，先保存当前进度，然后寻求帮助。